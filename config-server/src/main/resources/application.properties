spring.application.name=config-server
server.port=8888

# Local file-based configuration instead of Git repository
spring.cloud.config.server.native.search-locations=classpath:/configs,file:///configs

spring.profiles.active=native

spring.cloud.config.server.default-application-name=application

# Eureka registration
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
